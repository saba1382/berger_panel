export type product = {
    id: number | string,
    icon: any,
    title: string,
    weight: number | string,
    Ingredients: string[],
    price: string | number,
    see: boolean,
    setting: boolean
}